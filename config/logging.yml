version: 1
formatters:
  standard:
    format: "%(asctime)sZ | %(levelname)s | %(name)s | %(message)s"
handlers:
  console:
    class: logging.StreamHandler
    formatter: standard
    level: INFO
root:
  handlers: [console]
  level: INFO
