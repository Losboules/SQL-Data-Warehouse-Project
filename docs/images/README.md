# Diagram Assets

Each architecture diagram is supplied as editable Graphviz DOT, editable Mermaid, and rendered SVG. The SVG files are used by the README and can be viewed without Graphviz. The diagrams describe design and lineage; they do not claim an external platform run occurred.
